Associate a table in a non-customized environment without KerberosCloudera Docs
Associate a table in a non-customized environment without Kerberos
You can run a command to associate a table in a non-customized environment without
        Kerberos.

Run the following command to associate a table:

phoenix-psql ZooKeeper_hostname -m Schema_name.table_name



Parent topic: Associating tables of a schema to a namespace