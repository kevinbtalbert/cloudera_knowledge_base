User access to clustersCloudera Docs
User access to clusters
Access to Cloudera Manager and other cluster UIs and endpoints (such as JDBC) is always
    via the secure Knox gateway on port 443. Users are automatically logged in with their CDP as a
    Service credentials. 

