Creating a clusterCloudera Docs
Creating a cluster 
Let’s create a cluster from one of the basic definitions and then access its basic
  functionality: 


Create a clusterYou can quickly create clusters from default cluster definitions within an existing     environment.Monitor cluster creationOnce cluster creation has been triggered, you can monitor it from cluster details. Access cluster UIs and endpointsOnce your cluster is running, you can access cluster UIs and endpoints from the cluster   details page.SSH to the clusterTo connect to a cluster node through SSH, obtain the cluster node IP address and then     use either the user's SSH private key or your workload credentials. 